<!-- <%- include('partials/head') %>
<%- include('partials/navbar') %>

<div class="grid-containers">
    <div class="grid-items w-scale-2">
        <h1 style="color: white; text-align: center;">MASTER DATA</h1>
        <div class="d-flex flex-row-reverse">
            <button onclick="openAddModal()" class="btn btn-success btn-sm"><i class="bi bi-folder-plus"></i> Add</button>
        </div>
        <div class="table-container">
            <table class="fixed_header">
                <thead>
                    <tr>
                        <% if (data.length > 0) { %>
                            <% Object.keys(data[0]).forEach((key, index) => { %>
                                    <th><%= key %></th>
                            <% }); %>
                            <th>Actions</th> 
                        <% } else { %>
                            <th colspan="7">No Data Available</th>
                        <% } %>
                    </tr>
                </thead>
            </table>
            
            <div class="table-body">
                <table class="fixed_header">
                    <tbody>
                        <% if (data.length > 0) { %>
                            <% data.forEach(item => { %>
                                <tr>
                                    <% Object.keys(item).forEach((key, index) => { %>
                                        <td><%= item[key] !== null ? item[key] : '' %></td>
                                    <% }); %>
                                    
                                    <td>
                                        <button onclick="openEditModal(<%= item.id %>, <%= JSON.stringify({...item, id: undefined}) %>)" class="btn btn-warning btn-sm">
                                            <i class="bi bi-pencil-square"></i>
                                        </button>
                                        
                                    
                                        <button onclick="deleteRecord(<%= item.id %>)" class="btn btn-danger btn-sm">
                                            <i class="bi bi-trash"></i>
                                        </button>
                                    </td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="8">No records found</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<link rel="stylesheet" href="/styles.css">
<script src="/js/crud.js"></script>
</body>
</html> -->
